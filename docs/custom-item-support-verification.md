# HF6 커스텀 아이템 지원 감사와 구현 기준

2026-09-20. 사용자 평가의 빈 장비 슬롯·커스텀 제작·소모품·파열 같은 아이템 효과 지원을 끝까지 연결하는 후속 작업이다. **미완료**이며 이번 기록은 결함 재현과 다음 구현 범위를 고정한다. HF1–HF5와 전체 목표를 대신하지 않는다.

최신 소모품 후속: 최종 `096fb9d8…`에서 전체 회귀 단일 체인/61개 페이지 빌드와 신규 소모품 모델 제작 20건·사용 18건/7,547사건 재경기 일치가 종료 0이다. 실제 로컬 제품의 편집→새로고침→제작/사용→보관→x16 재경기에서 7,840사건·최종 상태·종료 판정이 일치하고 새 탭의 원본/조합식 복원도 확인했다. 아직 미커밋·미배포이며 계정/MongoDB 왕복과 장비 효과·후반 성장·원 경기 빈 슬롯 문제는 별도 미완료다. 아래 수정 전/중간 진행은 당시 기록이며 최신 근거는 `custom-consumable-verification.md`를 따른다.

## 수정 전 제품 경로

수정 전 기준 확인: `afb0c26fd5aeb9e032b5d90f9c6ff9fadbae9e58d0aaa7649a54b3a3845cacde`에서 진행하던 `npm run check:eternal-hunger` 단일 전체 체인이 2026-09-20 종료 코드 0으로 완료됐다(프로덕션 빌드 60/60 포함). 이 결과는 HF2 반영 버전의 회귀 기준이며 아래 HF6 미지원 기능의 합격을 뜻하지 않는다. 이후 추가한 제작 집중 검사는 실제 수량/비용·부분 수령·사망 제작 결함을 재현했으며 새 정산 API를 요구하는 항목은 당시 모듈 미구현으로 실패했다.

- `ItemEditorModal.jsx`는 이름·분류·슬롯·기본 능력치·조합식 비용/결과 수량을 저장한다. 소모품/발동 효과의 명시적 편집 필드는 없다.
- `server/models/Item.js`는 기본 정보와 ObjectId 재료 참조를 저장하지만 `consumeEffect`/장비 발동 효과 스키마는 없다. 임의로 보낸 효과 필드는 보존되지 않는다. 실제 계정 DB 변경이나 정리는 실행하지 않았다.
- 게스트의 `loadGuestSimulationItemCatalog()`는 내장 카탈로그를 복사한다. 계정 편집과 별도로 게스트 커스텀 카탈로그 저장/선택도 연결해야 한다. 캐릭터/지도 편집이 있다는 사실로 아이템 편집까지 있다고 판정하지 않는다.
- `applyItemEffect()`는 이름·태그 기반 기존 음식/영구 소모품/캡슐을 해석한다. 일부 명시적 회복량을 읽더라도 실제 획득 경로 `addItemToInventory()`에서 효과 필드가 빠진다.
- `tryAutoCraftFromInventory()`와 `tryAutoCraftFromLoot()`는 재료/수용량 방어가 있지만 결과 1개를 고정하고 조합식 크레딧 비용을 읽지 않는다.

## 수정 전 실제 함수 재현

기준 엔진: `afb0c26fd5aeb9e032b5d90f9c6ff9fadbae9e58d0aaa7649a54b3a3845cacde`.
실행: `client`에서 `node ./scripts/audit-custom-item-support.mjs`.
실제 스키마·분류·획득·사용·제작 함수를 일회용 메모리 입력으로 호출한다. DB 연결·계정 저장·설정 변경은 없다. 종료 코드 0은 감사 실행 성공일 뿐 지원 완료가 아니다.

| 식별자 | 입력/기대 | 실제 관측 |
| --- | --- | --- |
| HF6-SCHEMA | 명시한 회복량 25 저장 | 문서 변환 후 효과 없음; 비용 7·결과 3개는 보존 |
| HF6-CATEGORY | 편집기 분류 `소모품`, 이름 `맞춤 회복약` | 런타임은 재료로 분류, 자동 회복 후보 아님 |
| HF6-RECEIPT | 회복 25를 명시한 음식 획득 | 획득 전 25 → 가방 저장 후 기본 회복 18, 효과 유실 |
| HF6-USE | 지원하지 않는 효과 사용 거부·아이템 보존 | 강제 사용 성공, 회복 0·아이템 1개 소실. 자동 사용은 안 됨 |
| HF6-CRAFT | 약초 2개+7Cr → 결과 3개 | 재료 소비·결과 1개·20Cr 그대로. 0Cr도 성공, 획득 직후 제작도 결과 1개 |

수정 전에는 다섯 항목 모두 미지원/결함을 재현했다. 아래 후속 구현에서는 제작 수량/비용을 수정했고 나머지 네 항목은 미지원이다. 실제 원 평가의 캐릭터·카탈로그·경기 기록은 미확보다. 단검 T1이 없는 표본 카탈로그에서 시작 무기를 생성하지 않는 경계는 확인했지만 이를 원 경기 빈 슬롯의 원인으로 단정하지 않는다. 가상 장비를 지급하여 빈 슬롯을 감추지 않는다.

## 구현 순서와 완료 기준

1. **편집→저장→카탈로그의 단일 계약:** 계정/게스트의 식별자·분류·슬롯·조합식·명시적 효과를 같은 의미로 보존한다. ObjectId와 게스트 안정 키를 구분하고 내장 원본을 유지한다. 저장 실패·미지원 필드·잘못된 참조/순환 레시피를 명확히 알린다.
2. **실제 획득/제작 정산:** 지원 효과 보존, 재료 합계·결과 수량·크레딧·수용량의 원자적 처리. 실패/중복 실행/저장 복원 재시도에서 재료·크레딧·기존 장비를 보존한다. 실제 레시피만으로 제작한다.
3. **소모품 자동 판단과 적용:** 이름의 우연이 아닌 명시적 회복/보호/강화 계약으로 판단한다. 이유·실제 적용량·지속·소비 수량을 기록한다. 무효/미지원/사망/상태 제한이면 가짜 성공·아이템 소실이 없어야 한다. 기존 음식 효과를 묵시적으로 바꾸지 않는다.
4. **장비 효과의 실제 전투 연결:** 파열 등은 설명문만 저장하지 않는다. 발동 조건·수치·시간·중첩·면역·쿨다운·장착/해제·사망/부활 계약을 먼저 확정한다. 원작 의미/수치를 지어내지 않으며 커스텀 규칙과 원작 재현을 구별한다. 미지원 효과는 명확히 안내한다.
5. **빈 슬롯과 후반 성장:** 미설정, 카탈로그/슬롯/무기 불일치, 공급처 없음, 제작 대기, 장착 실패를 구분한다. 후반 선택→자원 경쟁→획득→제작/장착도 실제 상태로 추적한다. 원 평가의 빈 슬롯은 실제 입력 확보 시 재현한다.
6. **제품 왕복:** 실제 편집 저장→새로고침→편성/획득→재료·비용 소비→장착/효과→보관→동일 재경기. 배속/관전 변경 뒤 사건·프레임·최종 상태·난수·종료 일치, 원본 보존, 보상 중복 없음이 기준이다. 함수 검사·모의 저장만으로 완료 처리하지 않는다.

## 검증 경계

### 제작 경로 후속 구현 — 2026-09-20

엔진 `f711832bab1d755d648273c4f7ae5c16178f119bd3dc70d990029ac05ddd92c3`.

- 공통 `prepareCraftTransaction`/`commitCraftTransaction`은 유효한 재료 합계·양의 정수 결과 수량·0 이상의 정수 비용·현재 생존/잔액/가방을 검사한다. 수량/비용을 생략한 기존 조합식만 1개/0Cr 기본값을 유지하며 잘못 입력된 값은 조용히 기본값으로 바꾸지 않는다.
- 일반/큐 제작, 획득 직후 제작, 특수 재료 제작, 전투 후 제작에 연결했다. 복사본에서 결과 전체를 수령할 수 있는지 확인하므로 실패하면 재료·크레딧·교체될 장비·공간 확보용 재료가 보존된다. actor의 정산 순번과 준비 시 가방/잔액/행동을 재확인해 중복·오래된 결과 덮어쓰기를 차단한다. 같은 행동에서 서로 다른 실제 획득에 따른 연속 제작은 허용한다.
- 첫 파밍 경로와 현재 성장 계획은 조합식의 결과 수량을 사용하고 다른 분기에서 쓸 수 있는 남은 제작분도 계산한다. 실제 가방에 아직 없는 미래 결과물을 즉시 제작 가능하다고 취급하지 않는다. 부족 크레딧과 전체 결과 수령 불가를 관전에 표시한다.
- 제작 로그/관전/이벤트는 실제 수량·비용·전후 잔액·소비 재료를 보존한다. 계정 제출과 서버 기록 압축 경계에도 연결했다. 전투 후 제작에 없던 실제 제작 사건도 기록한다. 제품과 완주 검사에서 같은 획득 후 정산 함수를 사용한다.
- 집중 `check:custom-crafting` **23/23**: 수량/비용, 잘못된 값, 재료 합계, 부분 수령/장비 교체/자동 버림 실패 보존, 사망, JSON 복원/중복, 오래된 결과·타인 결과 거부, 큐/특수/PvP 경로, 성장 묶음 계산, 관전/계정 기록 포함. 실제 API/DB 저장이나 브라우저 편집 검사가 아니다.
- 관련 확인: 실제 내장 카탈로그 파밍 24명/8팀·53회 제작, 성장 16/16(통제 훈련 24명 완료), 무레시피 장비 차단 24건, 사냥 정산 19건, 거래 29건, 성장 관전 16/16, 팀 관전 17/17, 관전 성능 방어 8/8·계측 3/3·실시간 프레임 10/10·phase 4/4, 런타임 ESLint와 서버 보안 7건 통과. 프로덕션 빌드 60/60 종료 0. 함수·구조 성능 검사는 실제 FPS/heap을 대신하지 않는다.
- `audit-custom-item-support.mjs` 재실행에서 HF6-CRAFT는 직접/획득 후 모두 3개·20→13Cr, 0Cr 제작 거부를 확인했다. 다른 네 항목은 여전히 미지원이다.
- `check:custom-crafting:match` 종료 0. 실제 24인 경기 입력 중 3명에게 커스텀 재료/2단계 조합식·시작 비용을 명시한 통제 표본을 사용했다. 커스텀 제작 6건 모두 관전 모델에 표시됐고 첫 실제 장착은 경기 40초였다. 13페이즈·6,618프레임·7,829사건으로 실제 교전 종료 후 저장 입력을 복원한 두 번째 실행은 사건·프레임·최종 상태·난수·종료 판정이 일치했다. API 호출 0, 원 입력 불변도 확인했다. 사건 digest `7709da98c3417c85dd873211c2332e3c941c4e9a68010778d78e552a2e9c3d44`, 프레임 digest `8905787703b063b6497555ca139084afd0287d764ae644a4e833d6bd60b984b0`. 원 평가 경기나 실제 편집 저장의 대체물이 아니다.
- 같은 `f711832b…` 클라이언트 엔진의 `npm run check:eternal-hunger` 단일 전체 체인이 2026-09-20 종료 0으로 완료됐다. 제작 23/23, 실제 스킬/상태 완주·재경기, 난수 12/12·저장 입력 16/16, 타이머 21/21 및 모델 완주·재경기, 관전/지도/음원/런타임 검사와 프로덕션 빌드 60/60을 포함한다. 별도 미배포 소모품 서버 변경은 이 클라이언트 체인의 지원 완료 증거에 포함하지 않는다.
- 로컬 프로덕션 제품: 기준 시드 `1789192220412`·24인·x32로 경기 시간 21:22.83, 3팀 다온 승리·원본 보관을 확인했다. 관전에는 `대환단 1개 제작 완료`·`셀레네의 눈물 1개 제작 완료`와 성장 목표가 갱신됐다. 제품 보관함의 동일 재경기를 불러와 x16으로 바꾼 경기에서도 사건 7,590개·최종 상태·종료 판정 일치를 표시했다. 새로고침 후 20:20:48 원본은 유지됐고 재경기 기록은 중복 추가되지 않았다. 사람 평가 값은 입력하지 않았으며 임시 확인 탭과 직접 실행한 로컬 서버는 종료했다. 이 내장 아이템 화면 표본은 커스텀 효과 편집/저장 증거가 아니다.

원 경기의 빈 슬롯/마커스 고립 동일 재현과 사람의 재평가는 미완료다. 전체 자동 검사가 통과해도 위 미지원 기능이 구현된 것은 아니다. HF2의 공개/재경기 증거는 `movement-objectives-verification.md`에서 별도 관리한다. HF6 구현 뒤에는 변경 엔진으로 관련 회귀·성능·제품 화면을 다시 확인한다.

### 제작 수정 공개 반영과 다음 연결

- 제작 수정은 `9b6184f899e68f251261e7f9bbe6db4c75679a0c`를 main에 커밋/푸시했고 Vercel success를 확인했다. 익명 공개 평가 화면에서 한 번 시작한 기준 경기가 21:22.83, 3팀 다온 승리로 끝나 원본 보관을 표시했다. 임시 터널은 사용하지 않았다.
- 소모품은 별도 미배포 작업으로 효과 편집·기기 저장·카탈로그/가방·자동 사용·관전/기록까지 연결했다. 집중 27/27·기기 편집 9/9·성장 17/17, 중간 엔진 `90ec4264…` 신규 아이템의 실제 제작 20건·사용 18건과 완주/재경기를 확인했다. 제작 재료인 소모품을 파밍에서 제외하던 회귀는 별도로 재현/수정했다. 최종 `096fb9d8…` 전체 검사·제품 UI/배포 및 HF6 나머지 범위는 미완료다. 해시별 근거는 `custom-consumable-verification.md`를 따른다. 위 제작 버전의 '네 항목 미지원'은 공개된 이전 버전의 감사 결과이며 현재 로컬 소모품 변경과 구분한다.
