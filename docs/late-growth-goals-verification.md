# HF2/HF6 후반 성장 목표 — 실패 재현과 구현 경계

2026-09-22 기록 상태 갱신: 사용자가 마커스 원 파일이 없다고 확인했다. 아래 '기록을 찾아 첨부'는 당시 상태이며 더 이상 대기하지 않는다. 빈 슬롯은 새 경기/별도 통제 입력에서 검증하고, 원 경기의 정확한 원인은 확인 불가로 남긴다.

2026-09-21. **로컬 구현/검증 중·미배포**. 장비 발동 효과 `7611e7b7`/엔진 `22e7f47c…`의 공개 릴리스와 별개인 다음 작업이다. 사용자 의도는 팀이 실제 파밍·교전·생존을 이어 가고, 희귀 재료와 장비 목표가 그 판단 및 관전에 연결되는 것이다. 원 평가 경기 JSON은 아직 확보하지 못했으며 사용자가 기록을 찾아 첨부하겠다고 답했다. 아래 통제 사례를 원 마커스 경기나 원 빈 슬롯의 원인으로 단정하지 않고, 기록이 오면 원 입력으로 별도 재현한다.

## 수정 전 재현한 실패

수정 전 `node client/scripts/check-late-growth-goals.mjs` (저장소 루트 기준)는 **2/6 통과, 종료 1**이었다. 이 검사는 장비 효과 릴리스의 전체 검사에 포함되지 않았다. 현재 후속 구현에서 `check:growth-plan` 뒤 `check:late-growth`를 실행하도록 연결했으며, 기존 버전의 전체 검사 성공을 새 요구의 성공으로 세지 않는다.

시험 입력은 2구역, 이미 장착한 T4 모자, 명시한 `goalLoadouts.legend.headKey`, 실제 T4 모자+추가 천 1개+3크레딧이 필요한 T5 제작법이다. 원작 특정 아이템/수치를 주장하지 않는 커스텀 제작 사례이며, 크레딧 20에서 장비나 재료를 자동 지급하지 않는다.

| 경계 | 기대 | 실제 결과 |
| --- | --- | --- |
| 초기 장비 완성 후 계획 | 명시한 전설 목표 `late-selected`를 선택 | 목표 ID 빈 문자열 |
| 실제 제작법의 부족분 | 장착 모자는 유지·예약하고 천이 있는 b 구역을 추적 | 후반 목표 자체가 선택되지 않음 |
| 실제 행동 큐 | 명시한 전설 목표를 제작/조달 목적에 사용 | 카탈로그에서 먼저 나온 다른 T5 `late-other` 선택 |
| 관전 표시 | 선택된 후반 목표와 부족 재료 표시 | 초기 목표 완료로 판단하여 후반 목표 ID 없음 |
| 초기 미완성 | 초기 제작법 우선, 공짜 후반 장비 없음 | 통과 |
| 상위 제작법 없음 | 가상의 장비·재료·보상 없음 | 통과 |

당시 연결 단절은 다음 코드와 맞았다.

- `growthPlanRuntime.js`: `routePlanTargetItemIds`의 초기 목표만 계획하고 모두 확보하면 빈 목표를 반환한다.
- `craftRuntime.js`: `pickGoalLoadoutBySlot`이 `goalLoadouts.transcend`만 읽어 전설 목표 키를 사용하지 않는다. 일반 제작 후보 점수는 후반 목표 전체 계획의 대체가 아니다.
- `phaseActionQueueRuntime.js`: 초기 계획이 존재하면 그 계획의 빈 `missing/currentZoneItemIds`가 후반 제작 목표보다 우선해 필요한 재료 목록을 지운다.
- `growthObservationRuntime.js`: 초기 목표를 모두 보유하면 후반 목표를 검사하기 전에 완료 표시를 반환한다.

## 다음 구현의 종료 기준

1. 초기 완료 여부와 현재 후반 목표를 구분한다. 후반 장비를 노린다고 초기 파밍 미완료로 되돌려 합류를 지연시키지 않는다.
2. 사용자가 설정한 전설·초월 목표, 실제 무기/장착 부위·보유 장비·유효한 조합식을 사용한다. 초기 경로가 없는 커스텀 편성, 존재하지 않는/부적합한 목표, 공급처·안전 경로 부재를 구분한다.
3. 실제 가방에서 다단계 조합식·필요 수량·비용을 계산하고, 현재 착용한 하위 장비는 재료로 실제 소비할 때까지 효과를 유지한다. 무한 재료·완성 장비 보정이나 보이지 않는 추가 제작 행동을 넣지 않는다.
4. 선택 → 희귀 자원/일반 재료 조달 → 실제 수령 → 제작·장착 → 관전 기록을 연결한다. 공급 소진/금지구역/위험 후퇴 시 목표와 문구를 갱신한다.
5. 원본 보존·JSON 저장/재경기·배속/관전 불변, 실제 제품 화면과 최신 성능을 확인한다. 사람의 이해도·재미 평가는 별도로 받는다.

장비 효과 릴리스 전체 검사 및 빌드가 실행 중이던 실패 재현 단계에서는 계산 엔진을 수정하지 않았다. 장비 효과의 전체 통과/공개 확인 뒤 다음 성장 변경을 시작했으며 서로의 엔진/검증 증거를 섞지 않는다.

## 2026-09-21 첫 후속 구현과 집중 검증

- 초기 완료와 현재 `opening/late` 단계를 분리했다. 명시한 전설/초월 목표와 실제 카탈로그 제작법을 선택하고, 없는/다른 부위/무기 목표는 확인 필요로 표시한다. 명시하지 않은 슬롯은 보유 장비보다 한 단계 높은 실제 제작법만 자동 후보로 삼는다. 카탈로그 순서나 관전 난수를 사용하지 않는다.
- 실제 재료/비용 역산을 큐·획득 직후 제작·일반 재료 탐색·팀장 이동·관전에 연결했다. 현재 착용한 하위 장비는 예약해도 능력치/효과를 유지하고, 실제 제작 거래가 성공할 때 소비한다. 후반 목표가 있어도 초기 완료를 취소하지 않으며 합류 이동이 우선한다.
- 필요한 희귀 재료가 미스릴인데 관계없는 자연 코어로 강제 이동하던 우선순위를 제한했다. 공유 필드 재고·금지구역·실제 알파 출처를 사용하고, 완료 장비나 재료를 지급하지 않는다.
- 수량 0인 장비가 `invQty`에서 1개로 계산되던 실패를 재현·수정했다. 잘못된 수량은 목표 달성 근거로 쓰지 않고, 과거 `_id` 형태도 실제 제작 때 같은 식별자로 소비한다.
- 첫 구현에서 집중 검사 **20/20**, 기존 성장 관전 **16/16**, 합류 **22/22**, 팀 전술 **29/29**, 이동 목표 **29/29**, 실제 제작 계약 **23/23** 통과. 초기 성장 훈련도 실제 24인 **24/24**, 성장 검사 **17/17**로 통과했다. 변경 런타임·검사 ESLint 종료 0이다.
- 첫 엔진 `628defb3287715995a9f82f764e7cf0850a1b02f43bf41827adc48bda91207d9`의 실제 24인 모델/저장 입력 재경기는 **6,825사건·6,452프레임 및 최종 상태·난수·종료 판정 일치**, 종료 0이다. 처음 3명의 T4→T5→T6 레시피/목표를 명시한 시험 입력이며 원작 밸런스나 기본 카탈로그 성장률의 증거가 아니다.
- 추가 경계 감사에서 관전 계획의 `need`(남은 수량)와 조달기의 `need-have`(전체 요구량에서 보유분 차감)의 차이를 발견했다. 미스릴 4개 중 2개 보유 사례가 **0 !== 2**로 실패했다. 첫 모델 종료 후 조달기에 전체 요구량을 넘기도록 경계를 수정했으며 관전 계획은 변경하지 않는다. 최종 집중 검사는 **21/21 통과**다.

## 마지막 수량 수정 포함 모델·빌드

최종 엔진은 `06de6ab326b835a6522c6dc622972cca9fee6b98bf86f94ab05f710dcaec6b2b`다. 해당 버전으로 모델을 다시 실행하여 종료 0을 확인했다. 24명·일반 필드 공급/위험·시드 `HF2-late-growth-match`에서 커스텀 모자 T4/T5/T6의 실제 제작은 각각 **13/11/6건**이었다. 자동 목표를 선택한 다른 참가자의 제작도 포함하므로 처음 3명의 제작 횟수로 해석하지 않는다. 재료·장비·크레딧 지급이나 이동/교전 면제는 넣지 않았다.

- 처음 3명의 실제 장착 actor-frame 수는 T4 **606**, T5 **722**, T6 **8,541**이다. 서로 다른 프레임 수나 사람 수가 아니다.
- 실제 관전 모델에서 T5/T6 후반 목표와 기본 카탈로그의 후속 목표를 확인했다. 목표 목록은 선택한 한 개만 저장한다.
- **21:11, 7일 낮, 8팀 승리, 13페이즈, 6,825사건·6,452프레임**으로 종료했다.
- 관전 호출을 섞은 저장 입력 재경기의 사건·최종 상태·난수·종료 판정·프레임 해시가 모두 일치했다. API 호출 0, 원본 입력 불변이다.
- 사건 SHA-256: `011d29a32747fe41889b44c62085f58432cd84703a3d95a1a549cce132a74ffe`
- 프레임 SHA-256: `804e47f83935096ffdb7098630e5a583a2184da7f9a50d657e7c86d1679a50b9`
- 같은 엔진의 `check:runtime:sweep`와 프로덕션 빌드 **61/61페이지·종료 0**을 확인했다.

## 같은 엔진의 로컬 제품 관전

기존 3107 검증 origin의 기기 저장 편성/커스텀 장비/스킬을 유지하고, 프로덕션 서버에서 시드 `2026092111`의 새 24인 경기를 x32로 실행했다. 이전 파열 검사 때 저장한 커스텀 모자/3인 Q가 있으므로 순수 기본 편성 밸런스 표본이라고 부르지 않는다. 원 평가 JSON과도 별개다.

- 15:00 이후 7팀 관전에서 초기 목표 **5/5**와 동시에 솔빛 **타이탄 아머**, 새벽 **다비드 헤드폰**(머리띠 1개·미스릴 1개 부족/연못), 매화 **토템** 후반 목표를 실제 제품 DOM에서 확인했다.
- 같은 카드에 실제 드론 가죽 수령(-10Cr), 미스릴 구매(-250Cr), 생명의 나무 구매(-200Cr) 기록이 표시됐다. 이는 해당 표시의 증거이며 모든 제작 재료가 그 거래에서 왔다고 추정하지 않는다.
- 후속 표본에서는 레이더/부족 배터리, 다비드 헤드폰, 현재 목표 확보 후 다음 판단 대기를 확인했다.
- 원본은 **21:21, 7팀 3명 생존·매화 우승 표시**로 종료했고 시작 조건/전체 사건 보관 완료가 표시됐다.
- 그 결과의 `동일 조건으로 재경기`로 x16을 시작하고 관전 팀과 경기 현황 탭을 변경했다. 제품의 비교 판정은 **사건 7,364개·최종 상태·종료 판정 모두 일치**다.
- 새로고침 뒤 최신 원본 **2026-09-21 22:36:21·7팀·21:21·시드 2026092111**이 한 건으로 남았다. 기존 **9/21 21:29·6팀·21:12.75**, **9/20 23:00·3팀·20:52** 기록도 보존됐으며 옛 엔진 기록은 기존 계약대로 동일 재경기 실행 불가 안내를 표시했다. 삭제·덮어쓰기나 인간 평가 대리 입력은 하지 않았다.
- 실제 브라우저 경고/오류 목록은 비어 있었다. 로컬 서버 재기동 전 오류 문서에 남은 구 탭은 정상 같은-origin 새 탭에서 복구했고, 새로고침 직후 한 번의 조작 제한시간 초과는 현재 UI를 다시 확인한 뒤 보관함에서 검증했다. 이를 제품 콘솔 오류나 데이터 손실로 세지 않는다.

제품 확인 후 해당 프로덕션 서버를 정상 중단하여 `.next` 충돌을 피하고 `npm run check:eternal-hunger` 전체 회귀 단일 체인을 시작했다. 2026-09-22 재개 시 해당 실행 핸들은 없고 프로젝트 Node 프로세스와 3107 리스너도 남아 있지 않았다. 최종 종료 코드를 확보하지 못했으므로 단일 전체 체인의 통과로 처리하지 않는다.

## 2026-09-22 중단 후 검증 이어가기

- 계산 소스/생성 엔진은 여전히 `06de6ab3…`이며, 준비된 22개 파일 외 기존 서버 변경은 별도로 보존했다. 마지막 프로덕션 빌드 산출물 시각은 9/21 22:29로 유지돼 있다.
- 중단 전 확인한 강제 제어, 행동 상태, 시전 및 기기 캐릭터 완주/재경기 결과는 유지한다. 마지막 기기 캐릭터 검사는 19/19·8,343사건/6,861프레임과 최종 상태·난수·종료 판정 일치였다. 상태 스킬 20/20 및 원본 경기 10,317사건은 확인했지만 그 재경기 종료는 미확인이므로 해당 단계부터 다시 실행한다.
- `check:character-status-skills`부터 관전/프레임/타임라인/난수/저장 재경기/종료·타이머/기기 진행/지도·규칙/무기·스킬/피드백·음원/성능 방어선/런타임 검사까지 순차 재개했다. 이번 로그는 `artifacts/late-growth-regression-tail-20260922.log`에 저장한다. 실제 확인 서버와 `.next`가 충돌하지 않도록 빌드는 이 체인 뒤 별도로 실행한다. 완료 전에는 이전 부분 결과와 합쳐 전체 통과를 주장하지 않는다.
- 재시작한 같은 origin의 실제 제품 보관함에는 9/21 22:51:53·시드 1101·7팀·18:14.25 원본 한 건과 위 세 이전 원본이 모두 남아 있었다. 중단 전 완료한 x8→x16→x1→x32 재경기는 5,851사건·최종 상태·종료 판정 일치를 표시했으며, 이후 중단된 재경기로 중복 기록이 추가되지 않았다. 사람의 평가는 네 기록 모두 미시작이다.

잔여 회귀 체인은 2026-09-22 `VERIFICATION_EXIT_CODE=0`으로 종료했다. 상태 스킬 20/20 및 10,317사건/7,011프레임 재경기 일치, 관전 17/17, 이동 목표 29/29, 성장 관전 16/16, 프레임 10/10, 행동 시간축 10/10·진단 9/9, 난수 12/12, 재경기 16/16, 종료 13/13, 타이머 21/21 및 실제 6,148프레임/6,804사건 경기, 기기 진행·지도·무기·스킬·음원·피드백·성능 방어선·런타임 검사가 포함된다. 중단 전후를 합친 **분할 검증**이며 처음부터 끝까지 중단 없는 단일 전체 체인이라고 쓰지 않는다.

### 재경기 검사의 관전 입력 누락 보강

위 재경기의 사건/최종 상태/난수/종료 판정/프레임 일치는 유효하지만 출력의 `externalAccess: 379`를 발견했다. 외부 접근 0 단언이 첫 경기 뒤에만 있어, 두 번째 관전 혼합 재경기의 저장소 접근을 검사하지 못한 시험 결함이다. 따라서 그 출력으로 외부 접근 0을 주장하지 않는다.

- 실제 제품 `SimulationMainStage`는 `settings`와 `forbiddenIds`를 관전 모델에 전달하지만, 모델 시험 도구는 두 값을 생략했다. 규칙 스냅샷이 없는 관전 모델은 기존 로컬 규칙 호환 경로를 조회한다. 최소 재현에서 설정 생략 시 저장소 읽기 1회, 저장된 규칙 스냅샷 전달 시 0회였다.
- 시험 도구에 실제 제품과 같은 설정/금지구역을 전달하고, 두 번째 재경기 뒤에도 외부 접근 0을 단언하도록 보강했다. 저장소 계수를 숨기거나 예외를 허용하지 않는다. 이 변경은 시험 도구뿐이며 계산 엔진 `06de6ab3…`은 유지된다.
- 보강한 `check:simulation-replay`는 16/16, **7,262사건·6,452프레임·최종 상태·난수·종료 판정 일치, 외부 접근 0회**로 통과했다. 사건 해시 `7ee1ad6fb05bb12568656f15b52baf1e002bcce70f3ad3f9d75796d304b4a996`, 프레임 해시 `ab0413c03b953ef5dc2476747084d7f7944bed2f20d8a44e928c75f4bd3fac70`다. `check:simulation-random` 후속 실행과 함께 `artifacts/replay-observer-input-regression-20260922.log`에 기록하며 전체 종료 코드는 별도 확인한다.
- 이어진 난수 검사도 12/12, 기준/관전·배속 혼합/페이즈 상태 갱신의 3경기 **7,410사건·6,364프레임·최종 상태·기록·난수 일치, API 0회**로 통과했다. 보강 검증 체인의 `VERIFICATION_EXIT_CODE=0`을 확인했다. 기존 장비 효과 엔진의 결과를 새 성장 엔진 증거로 대신하지 않는다.

### 관전 중 시드 설정 공간 축약

- 실제 경기 화면에서 변경 불가능한 시드 편집 폼이 세로 127.78px를 사용했다. 시작 전에는 기존 편집을 유지하고, 경기 시작/동일 재경기/종료 뒤에는 시드와 조건 안내를 접힌 한 줄로 표시한다. 계산·시드 값·보관 규칙을 바꾸지 않는 표시 변경이다.
- 집중 시드 검사는 수정 전 실패를 확인한 뒤 9/9, 평가 스키마·링크 계약과 변경 파일 ESLint 종료 0을 확인했다. 최신 UI를 포함한 프로덕션 빌드는 **61/61페이지·종료 0**이며 `artifacts/late-growth-ui-build-20260922.log`에 남겼다.
- 같은 origin의 1280×720 실제 제품에서 경기 전 시드 입력 활성화·축약 영역 없음, 보관 경기 복원 후 입력 없음·접힌 높이 **36px**, Enter로 조건 안내 펼침·Space로 다시 접기를 확인했다. 약 **92px**의 설정 높이를 줄인 증거이며 지도 전체 가독성이나 모바일 합격으로 일반화하지 않는다.
- 보관함의 네 원본과 엔진이 다른 두 기록의 재경기 차단 안내도 유지됐다. 기록 삭제·인간 평가 대리 입력은 하지 않았다.

## 공개 반영 — 2026-09-22

- 구현 커밋 `6b860cf8eb5d2ab1e56e04ab7657975692d8ad3c`를 main에 푸시하고 Vercel `success`를 확인했다. 배포: https://vercel.com/kurokosis-projects/eternal-hunger/D2Y8Pum5BpEW885W7aiMDxBwEcn2
- 익명 공개 `/eternalhunger`에서 시드 `1790079258131`·24명·8팀의 기존 시작 편성을 수정하지 않고 x32로 진행했다. 보관함은 시작 전 5건으로, 이 확인 경기 때문에 10건 상한의 기존 기록 제거가 발생하지 않았다. 원 마커스 평가 경기와는 별도 표본이다.
- 경기 시작 후 새 시드 요약이 접힌 채 표시됐다. **12:40의 2팀**에서 바람은 초기 5/5와 **가디언 슈트**(전신 수영복 1·운석 1·생명의 나무 1, 모래사장), 여울은 초기 5/5와 **가시지네 견갑**(붕대 1·운석 1, 공장)을 실제 제품 DOM에 표시했다. 10:50 운석 구매(-200Cr, 428→228)와 08:40 생명의 나무 구매(-200Cr, 349→149)의 실제 거래 카드도 관측했다. 구매가 곧 해당 목표 완성이라는 뜻은 아니다.
- **16:33.67·교전 종료·7팀 다온 표시·2/3명 생존·LP 0**, 시작 조건/전체 사건 보관 성공을 확인했다. 새로고침 뒤 **9/22 21:16:40 원본 한 건과 기존 다섯 건**이 남았고 새 기록의 동일 재경기가 활성화됐다. 기존의 평가 진행 상태도 보존했고 사람의 점수/메모는 입력하지 않았다.
- 관측 콘솔 경고/오류 0건. 새로고침 직후 한 번의 확인 도구 3초 제한시간 초과는 후속 정상 페이지와 보관함으로 확인했으며 제품 데이터 손실이나 콘솔 오류로 세지 않는다.
- 공개에서는 같은 경기의 재경기를 다시 완주하지 않았다. 재경기 일치는 위 로컬 제품/모델 표본의 증거와 구분한다. 이 배포는 후반 성장/UI 반영 증거이지 장시간 성능, 모든 희귀 필드 목표, 원 경기 빈 슬롯, 실제 계정 저장 또는 인간 수용성의 합격 선언이 아니다.

최신 장시간 성능, 자연 희귀 자원/보스 목표 제품 추적, 실제 계정 저장, 새 입력의 합류/빈 슬롯 검증 및 인간 재평가는 아직 미완료다. 원 평가 경기 JSON은 사용자가 파일 부재를 확인하여 더 이상 기다리지 않으며, 그 경기의 정확한 원인은 현재 자료로 확인 불가다.
