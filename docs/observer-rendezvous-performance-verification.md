# 합류 수정 이후 관전자 성능 재측정

2026-09-24. `a867b1e6` / 엔진 `94fac36f4488dbe7e7ec80167add14d991633da42998407f85ff0584b43905b3`의 실패 재측정부터 진단 `010820a1…`, 종료 처리 수정 `a64d82ae…`까지의 실제 제품 브라우저 증거다. 최신 x32 한 표본은 Long Task 최대 160ms로 기준을 통과했지만 최신 x1/x8·반복 heap·사람의 재평가는 미완료다. 이전 버전/입력의 성능 합격을 다른 입력/최신 엔진 전체의 합격으로 이어 붙이지 않는다.

## 동일 조건 연속 x8 세 경기

로컬 production 3108, Codex IAB 1280×720, 내장 21지역·24명/8팀·시드 `1789192220412`·스킬 켬·1팀/팀 관전이다. 19:41:55에 저장한 같은 원본을 보관함에서 세 번 복원했다. 각 실행은 측정 시작→오토, 결과 모달 닫기→측정 중지 순서다. 사이에 새로고침·기록 삭제·강제 GC를 하지 않았으며 무거운 검사/빌드도 겹치지 않았다. 두 번째 중간 화면에서 지도 섬 전체와 최근 교전 HP 표시를 확인했다. 사람의 가독성 합격은 아니다.

| 실행 | 측정 창 | Long Task 수 / 합 / 최대 | LoAF 최대 | RAF median / p95 / max | heap 시작 → 끝 |
| --- | ---: | --- | ---: | --- | --- |
| x8 1 | 378.4636초 | 52 / 5,379 / **302ms** | 314.2ms | 10 / 10.7 / 300ms | 30,631,239 → 40,934,255B |
| x8 2 | 269.4886초 | 50 / 4,703 / **451ms** | 452ms | 10 / 10.2 / 450.1ms | 57,228,373 → 41,838,525B |
| x8 3 | 247.3619초 | 51 / 3,852 / **227ms** | 237.6ms | 10 / 10.2 / 230ms | 58,137,974 → 42,214,129B |

- 세 번 모두 **20:52·4팀 매화 1/3명 생존·LP 0**, 제품의 **7,297사건·최종 상태·종료 판정 일치**를 확인했다. 보관함은 전후 2건이며 이전 엔진 기록의 재경기 차단, 평가 미시작을 유지했다. 최종 console warning/error 0, 측정 중지·JSON/보관함 접기까지 확인했다.
- 측정 창은 종료 후 체류를 포함한다. 첫 실행은 종료 후 확인이 늦었으므로 측정 창을 경기 실행 시간이나 실행 간 속도 비교에 쓰지 않는다. 세 실행 RAF 표본은 35,955 / 26,021 / 24,113개, 종료 DOM은 모두 662개, visibility는 `visible`이다. click→다음 RAF는 각 2표본, 최대 8.3 / 8.1 / 6.5ms이며 일반 입력 지연·INP·paint 완료의 대체 지표가 아니다.
- **개별 Long Task <200ms 관문은 세 번 모두 실패**다. 마지막 실행/평균 RAF만 골라 합격으로 바꾸지 않는다. 시작 heap도 30.63→57.23→58.14MB로 증가했으므로 메모리 관문은 미확정이다. 두 번째/세 번째 종료 heap이 시작보다 약 15MB 낮아진 사실과 종료 DOM 동일성만으로 누수 또는 누수 부재를 확정하지 않는다. 별도의 자료 수명/자연 회수 근거가 필요하다.

원자료 `artifacts/observer-rendezvous-sequential-browser-20260924.json`은 제품 진단 패널에서 읽은 집계·모든 작업 단계와 실행별 가장 느린 LoAF 두 건이다. 전체 trace가 아니며 실제 DOM 자료와 전사 체크섬을 대조했다. 경기 원본을 삭제하거나 평가 답변을 만들어 넣지 않았다.

## 원인 축소와 아직 모르는 부분

`growth.allActors`는 매 경기 67회다. 최대 동기 작업은 231.2 / 427.5 / 129.9ms다. 특히 두 번째의 측정 시작 102,348ms 시점 작업은 427.5ms로, 102,334.7ms에 시작한 452ms LoAF 안에 들어간다. 이 표본에서 전체 성장 판단 구간이 큰 비중을 차지한다는 직접적인 근거다. 하위 단계의 최대값들은 서로 다른 시점일 수 있으므로 더해서 한 작업의 내역처럼 만들지 않는다. GC/운영체제 스케줄링 시간을 별도로 분리한 프로파일은 아니다.

종료 부근에는 세 번 모두 별도의 긴 scheduler continuation이 남는다: 시작 offset 225,765.4 / 215,746.4 / 214,742.3ms, LoAF 314.2 / 220.9 / 237.6ms. 이 귀속은 진입점이지 전체 호출 스택이 아니다. 기존 진단에는 종료 기록 복사/비교가 없어, 이 자료만으로 특정 함수가 전부를 사용했다고 단정하지 않는다.

## 후속 계측 계약

실험체 한 명 처리와 이동·획득·계획·큐 실행, 종료 기록 복사·비교에 고정 이름의 진단 지점을 추가한다. 기존 query-gated recorder가 꺼져 있으면 시계를 읽지 않는다. 실험체 객체·기록을 계측기에 보관하지 않고, 기존 24종 작업 단계/8개 느린 프레임 상한을 유지한다. 처리 순서·게임 시각·난수·판단·전투·사냥 R 금지·동기식 종료 결과 캡처는 변경하지 않는다.

계측 활성/비활성 모델의 전체 사건·프레임·난수 동일성을 확인한 뒤 실제 제품에서 하위 단계와 종료 작업을 다시 측정한다. 이는 **진단 보강이지 지연 수정이나 성능 합격이 아니다**. 그 결과를 근거로 중복 계산 축소 또는 원자적 상태 게시/난수 격리를 보존하는 작업 분할을 결정한다. 초기 목표와 다른 HF1–HF7 잔여·사람의 재평가는 계속 미완료다.

### 진단 보강 검사

최종 진단 엔진은 `010820a1e6e70e2e61323c8db3b3fba4925506a126e81471b2f55a5f7d3e4982`다. 진단 19개, 단계/지역/yield 21개, 난수 12개, 저장 입력 재경기 16개, 사냥 26개로 집중 검사는 **94개·종료 0**이다. 변경한 runtime 3개와 검사 파일의 ESLint, 61/61 production build도 종료 0이다. 전체 프로젝트 회귀 체인을 새로 돌렸다는 뜻은 아니다.

- 실제 모델의 계측 비활성 기준 경기와 계측 활성/관전 입력 혼합 두 경기는 7,410사건·6,364프레임·난수·최종 상태·로그가 일치했다. 새 실험체별 5개 단계가 실제로 호출된다는 assertion을 추가했다. 사건 digest `980bb1827f9a3e6c2953b55f5c17d0ceed30dc5bdfadb48e8293caaff1a30f4b`, 프레임 digest `2ccfb7b0f77072c8c367a31177fe1a14662beb1bc8edde40c0f89483b9fe5a00`다. 이 모델은 실제 브라우저의 wall-clock 성능 증거가 아니다.
- 최종 생성 버전의 저장 입력 모델 재경기는 7,311사건·6,261프레임·20:25.75·1팀 종료가 같고 사건/최종 상태/난수/종료 판정 일치, 외부 API/계정 접근 0회다. 사건 digest `809d7466910fa1c98adccc5a18035baa2152931b69a49377de9eea24906e9e3f`, 프레임 digest `f304ecd383cb68bbe6c2d57aa3d338e8253a11f4c348da0f27e40c778e0cba9c`다.
- 사냥 26/26은 일반·변이·보스와 자신/아군 지원 R·재시전의 사냥 금지, PvP에서 준비한 R 강화 소모 방지 및 이후 적 팀에 실제 R 적중을 포함한다. 사용자 정책을 변경하지 않았다.

브라우저 연속 측정을 중지한 뒤 위 검사/빌드를 실행했다. 작업 전용 서버 PID 44092의 명령행/경로/3108번 포트를 확인하여 중지한 후 종료를 재확인하고, 같은 포트에 새 production 서버를 시작했다. 다른 Node/사용자 프로세스나 원본 기록은 종료/삭제하지 않았다.

### 실제 하위 단계 귀속

진단 엔진 `010820a1…`의 평가 경기를 새 원본으로 저장(20:29:03)한 뒤 정상 관전 페이지의 진단 패널로 x32 재경기했다. 원본은 계측하지 않았고 재경기 중에는 검사/빌드를 겹치지 않았다. **7,297사건·최종 상태·종료 판정 일치**, 20:52·4팀 1/3명·LP 0·원본 3건 유지·평가 미시작·콘솔 오류 0이다. 원자료는 `artifacts/observer-replay-completion-before-20260924.json`이다.

- 측정 창 203.5537초(종료 후 체류 포함), Long Task 55건/합 6,294ms/최대 **237ms**, LoAF 최대 **248.9ms**로 여전히 실패다. heap 31,904,580→43,152,106B, DOM 662, 입력 2표본 최대 7.3ms다. x32 한 번은 앞선 동일 x8 세 번의 메모리 관문 대체 자료가 아니다.
- 가장 긴 종료 LoAF는 offset 149,713.8ms부터다. 그 안에서 `replay.captureResult`는 149,730.5ms부터 **55.5ms**, 직후 `replay.compareResult`는 149,786ms부터 **175.7ms**다. 두 단계는 중첩이 아니라 연속 실행이며 합 231.2ms로 긴 종료 작업의 대부분을 직접 설명한다.
- 전체 성장 판단 최대 **190.2ms**, 한 실험체 처리 최대 **37.9ms**다. 이동/획득/계획/큐 실행은 각각 922회, 최대 20.4/16.8/16.9/33.9ms다. 이 표본에서는 한 실험체의 특정 단계보다 여러 실험체의 연속 처리 비용이 크다. 앞선 427.5ms 표본의 하위 분포까지 재구성했다는 뜻은 아니다.

## 종료 결과 처리 수정

`captureSimulationReplayResult`는 사건 배열을 의미 정규화하며 한 번만 JSON 소유 복사하고, 최종 프레임/난수/종료 자료를 따로 소유 복사한다. 사건 배열을 감싼 결과 객체를 다시 직렬화하던 중복을 제거했다. 캡처는 여전히 동기식이며 첫 await/저장 전에 완료된다.

비교는 동일한 `semanticRunEvents` 정규화 이후 사건들의 JSON 구조를 직접 읽는다. 이벤트별 중첩 객체 키를 정렬하고 전체 문자열을 만들던 작업을 없앴다. 객체 키 순서는 무관하지만 배열 순서·길이·own key·값은 모두 비교한다. top-level `ts`만 제외하는 규칙, 첫 차이 인덱스/양쪽 사건 수, 최종 프레임·난수·종료 판정의 기존 canonical 의미, 기록 버전 검사는 유지한다. 조회 중 어느 입력도 변경하지 않는다.

새 `check-replay-comparison-performance.mjs`는 변경 전 알고리즘을 oracle로 고정했다. 소유권/동기성, 전체 사건 직렬화 1회, timestamp 범위, JSON scalar·배열·누락/자체 키·길이 차이, 종료 판정, 잘못된 JSON 입력, 5,000번째 이후를 포함한 8,000사건, 제품 캡처 연결의 **10/10**을 확인했다. 자동 검사는 의미/구조 근거이며 실제 지연 개선은 후속 제품 측정으로 판정한다.

최적화 엔진은 `a64d82ae9c869757f75e9a46a3f2638d07e6eb11a19b2656e8ade9fa265bdbdf`다. 최종 변경 뒤 새 비교 10개·저장 입력 재경기 16개·진단 19개·사냥 26개로 **후속 집중 71개**, 변경 runtime 4개/검사 2개의 ESLint 및 61/61 production build를 종료 0으로 확인했다. 저장 입력의 7,311사건·6,261프레임·난수·종료 판정은 위 진단 버전의 모델과 같고 외부 API/계정 접근은 0회다. 앞선 계측 검사의 94개와 겹치는 검사를 더해 별개의 총 검사 수로 부풀리지 않는다.

## 최종 엔진의 실제 x32 재경기 — 이 표본의 성능 관문 통과

빌드 완료 뒤 해당 작업 서버의 PID 45932·명령행·경로·3108 포트를 확인하여 중지/종료를 재확인하고 최종 production 서버를 시작했다. 최종 평가 원본은 20:44:09에 저장했고, 같은 정상 관전 페이지에서 보관함 재경기를 x32로 측정했다. 원본은 계측하지 않았으며 검사/빌드·강제 GC·기록 삭제를 측정과 겹치지 않았다.

| 실제 측정 | 진단 버전 `010820a1…` | 수정 버전 `a64d82ae…` |
| --- | ---: | ---: |
| 종료 기록 소유 복사 | 55.5ms | 37.6ms |
| 전체 재경기 비교 | 175.7ms | 103.2ms |
| 위 두 연속 동기 작업 합 | 231.2ms | 140.8ms |
| 전체 경기 Long Task 최대 | 237ms | **160ms** |
| 전체 경기 LoAF 최대 | 248.9ms | **169.9ms** |

- **7,297사건·최종 상태·종료 판정 일치**, 20:52·4팀 매화 1/3명·13처치·LP 0이다. 엔진 호환 검사를 우회하지 않고 각 버전에서 같은 평가 입력/시드로 새 원본을 만든 뒤 그 버전 안에서 재경기를 비교했다. 비교표는 같은 PC/입력/배속의 두 실제 표본이며 일반적인 가속 비율이나 모든 실행의 보장을 뜻하지 않는다.
- 측정 창 193.5782초는 종료 후 체류를 포함한다. Long Task 54건/합 4,971ms, LoAF 62건/합 5,811.4ms, RAF 표본 17,378개·interval median/p95/max 10/20/169.9ms다. RAF 빈도 89.773은 실제 painted FPS가 아니다. 입력 2표본(오토·결과 닫기) 최대 7.9ms는 실시간 탭 전환이나 INP의 대체 증거가 아니다.
- 가장 긴 종료 LoAF는 offset 120,190ms부터 169.9ms(work 169.3/render 0.6/layout 0.1ms)다. 그 안에서 캡처는 120,214.9ms부터 37.6ms, 비교는 120,252.5ms부터 103.2ms로 연속 실행했다. 전체 성장 판단 67회/합 4,858.6ms/최대 156.5ms, 실험체 처리 922회/최대 36.5ms다. 이동/획득/계획/큐 최대는 각각 7.7/4.6/11.4/31ms이며 서로 다른 시점의 최대값을 한 작업처럼 합하지 않는다.
- heap **31,622,209→41,386,918B**, DOM 662, visibility `visible`이다. 한 경기 종료 heap 증가만으로 누수 또는 회수 성공을 판정하지 않는다. 최신 엔진의 동일 조건 연속 세 경기와 자료 수명/자연 회수 관문은 남는다.
- 측정을 중지한 뒤 새로고침하여 **원본 4건 보존·모두 평가 미시작·최신 재경기 활성/이전 엔진 재경기 차단**을 확인했다. 콘솔 warning/error 0이다. 새로고침 도구가 한 번 시간 초과를 반환했지만 같은 탭을 다시 읽어 GAME READY와 기록을 확인했으므로 서버/경기를 다시 실행하거나 실패로 집계하지 않았다.

원자료는 `artifacts/observer-replay-completion-before-20260924.json`과 `artifacts/observer-replay-completion-after-20260924.json`이다. 두 파일 모두 전체 집계/작업 단계와 가장 느린 LoAF 두 건만 남긴 진단 발췌다. 실제 DOM에서 읽은 JSON과 저장용 자료의 정렬 직렬화 전사 체크섬(각 4,009자/2,376,195,714 및 3,712자/801,839,954)을 대조했다. 이 값은 전사 오류 확인용이며 암호학적 무결성이나 게임 결과 증명이 아니다.

**이 x32 표본은 Long Task <200ms를 통과한다. G4/HF1 또는 전체 목표 완료는 아니다.** 최신 x1/x8, 동일 조건 연속 heap/자료 회수, 성장 계산의 반복 최대값, 원작 지도 통행·자연 희귀 목표·다양한 합류/빈 슬롯·실계정 저장·사람의 재평가를 다음 작업으로 유지한다. 앞선 x8 세 실패와 heap 증가 근거도 삭제하지 않는다.
