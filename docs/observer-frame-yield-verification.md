# 관전 시작의 결합 지연 분리

2026-09-24. 앞선 `e4347eb3…` 동일 입력 x8 세 경기에서 개별 Long Task는 189/196/175ms였지만, 시작 작업이 같은 긴 프레임에 합쳐져 LoAF 256.1/199.5/270ms를 기록했다. 원자료는 `artifacts/observer-sequential-x8-browser-20260924.json`이다. 평균 RAF 빈도나 개별 작업 합격으로 이 지연을 지우지 않는다.

## 변경 계약

- 기존 `scheduler.yield()` 경로는 작업을 나누지만 계속 실행할 작업을 우선 예약한다([Chrome 공식 설명](https://developer.chrome.com/blog/use-scheduler-yield)). 이번 관측은 준비 작업과 첫 성장 작업이 별도 task여도 같은 LoAF에 귀속되는 경우다.
- 페이즈 준비 직후에만 `requestSimulationFrameYield`를 사용한다. 보이는 문서는 RAF 콜백을 거친 뒤 별도 timer task에서 이어 간다. RAF 자체는 다음 repaint 전에 호출되므로 그 콜백에서 Promise를 바로 완료하지 않는다([MDN](https://developer.mozilla.org/en-US/docs/Web/API/Window/requestAnimationFrame)). 이는 화면 갱신 기회이며 실제 presentation 완료 보장은 아니다.
- 숨김/RAF 미지원 환경은 기존 scheduler 또는 0ms timer 경로를 유지한다. 대기 중 숨김 전환, RAF 요청 실패, 콜백 미도착에 대한 100ms fallback timer를 두고, 완료 시 RAF·두 timer·visibility listener를 정리한다. 실제 타이머 실행은 브라우저 스케줄링/백그라운드 제한을 받으므로 100ms 이내 완료를 보장한다는 뜻이 아니다.
- 잦은 행동 프레임과 페이즈 마무리는 기존 yield를 유지한다. 원자적 상태 게시 위치, 난수, 게임 내 시간, 처리 순서, 전투/파밍 수치와 궁극기 사냥 금지는 변경하지 않는다. 저장 기록의 엔진 버전 검사는 그대로다.

## 집중 검사

`check-simulation-cooperative-yield.mjs` 10/10: headless 무예약, RAF 안에서 미재개, RAF 이후 task 재개, 0번 handle, 숨김/미지원 fallback, 대기 중 숨김, 콜백 미도착, RAF 후 timeout 경합/중복 차단, 요청 실패, 예약 정리, 난수 격리·순서·게임 시각 불변, 실제 페이즈 연결을 확인했다. `check:phase-runtime-performance`에 연결했으며 기존 단계 성능 7/7·지역 매칭 4/4도 통과했다. 처음 실행은 새 helper 미구현으로 실패했으며, 실제 기존 지연 재현 근거는 위 브라우저 표본이다.

단계/지역/yield 21개, 난수 12개, 저장 입력 재경기 16개, live frame 10개, 사냥 26개, 진단 19개로 집중 검사는 합계 **104개**다. runtime ESLint sweep에서 listener 선언 순서 위반 1건을 발견하여 선언을 앞당겼으며, 이후 집중 단계 21개·runtime sweep·새 검사 파일 ESLint·61/61 production build는 종료 0이다. 최종 생성 엔진은 `658f658217f6acf8ef43d8e0d5a81d99fd5344cb24a390a558ee63b7f83b1604`다.

최종 엔진에서 난수 검사 12/12와 저장 입력 재경기 16/16을 다시 종료 0으로 확인했다. 난수 격리 모델 3경기는 7,410사건·6,364프레임, 저장 입력의 실제 모델 재경기는 7,262사건·6,452프레임·외부 API/계정 접근 0회다. 각각 수정 전 사건/프레임 digest와 동일하다.

- 난수 모델 사건 `980bb1827f9a3e6c2953b55f5c17d0ceed30dc5bdfadb48e8293caaff1a30f4b`, 프레임 `2ccfb7b0f77072c8c367a31177fe1a14662beb1bc8edde40c0f89483b9fe5a00`.
- 저장 입력 모델 사건 `7ee1ad6fb05bb12568656f15b52baf1e002bcce70f3ad3f9d75796d304b4a996`, 프레임 `ab0413c03b953ef5dc2476747084d7f7944bed2f20d8a44e928c75f4bd3fac70`.

모델 경로는 headless이므로 실제 RAF/timer 대기를 실행하지 않는다. 위 수치는 동작/수명·난수·결과 계약의 자동 검사이며 실제 브라우저 지연 개선이나 전체 목표 완료를 뜻하지 않는다. 전체 프로젝트 회귀 체인을 새로 실행한 것도 아니다. 제품 원본/재경기 측정은 아래에 별도로 기록한다.

## 실제 제품 브라우저 결과

동일 PC/IAB 1280×720, 로컬 production 3107, 내장 21지역·기기 저장 사용자 24명/8팀·시드 1101·스킬 켬이다. 최종 빌드 후 새로고침하고 x32 원본을 저장한 뒤 같은 원본을 x8, x32 순으로 복원했다. 측정 전후 설정/원본은 유지했고 무거운 검사·빌드를 겹치지 않았다. 재경기 사이 새로고침·강제 GC는 하지 않았다. 측정은 오토 전에 시작하고 결과 모달을 닫은 뒤 중지했으므로 시간에는 완료 뒤 체류가 포함된다.

| 실행 | 측정 시간 | Long Task 수 / 합 / 최대 | LoAF 최대 | RAF median / p95 / max | click→다음 RAF 표본 / 최대 | heap 시작 → 끝 |
| --- | ---: | --- | ---: | --- | --- | --- |
| 원본 x32 | 93.2709초 | 33 / 2,649 / **145ms** | **149ms** | 10 / 10.1 / 149.9ms | 2 / 5.2ms | 19,089,592 → 40,895,813B |
| 동일 재경기 x8 | 214.5658초 | 32 / 2,601 / **135ms** | **141ms** | 10 / 10.1 / 130.1ms | 2 / 1.6ms | 31,827,951 → 41,010,425B |
| 동일 재경기 x32·탭 전환 | 84.6321초 | 32 / 2,571 / **134ms** | **153.6ms** | 10 / 10.1 / 139.9ms | 7 / 8.2ms | 35,617,891 → 41,268,048B |

- 세 경기 모두 **18:14.25·7팀 생존 2/3명·LP 0**, 두 재경기는 제품에서 **사건 5,851개·최종 상태·종료 판정 일치**를 확인했다. 원본은 17:50:10에 저장되었고 기존 7건 + 새 원본 1건 = 8건이다. 재경기는 기록을 늘리지 않았다. 예전 7건의 엔진 호환성 차단은 유지된다. 마지막 콘솔 warning/error 0, 인간 평가 입력 없음, 측정 중지/JSON 접기까지 확인했다.
- 원본 시작의 준비/렌더 LoAF는 시작 370.5ms·길이 136.8ms, 첫 성장 LoAF는 시작 509.2ms·길이 149ms로 **서로 다른 프레임**이다. x8 첫 성장 LoAF도 128.2ms이며 이후 최대 141ms다. 기존 x8에서 반복된 결합 시작 270ms는 이번 x8/두 x32 표본에서 재현되지 않았다. 이후 x32 최대 153.6ms는 여전히 두 작업이 합쳐진 프레임이므로 모든 결합/끊김 제거라는 뜻은 아니다.
- 마지막 x32 경기 중 경기 현황→전체 로그→킬→요약→팀 관전으로 실제 전환했다. 측정 클릭 7건은 오토·5개 탭·결과 닫기다. 생존 인원을 포함한 팀 선택 문구가 3/3→2/3으로 변해 선택 시도 하나가 시간 초과됐고, **경기 중 팀 변경 성공으로 계산하지 않았다**. 7팀 선택은 종료/측정 중지 뒤 확인했다. 서버나 경기 실행을 재시작하지 않았다.
- 개별 Long Task <200ms 관문은 세 표본 모두 통과한다. RAF 빈도는 실제 painted FPS, 입력 표본은 INP/paint 완료, LoAF는 Long Task와 같은 지표가 아니다. 다른 기기·편성에 대한 보편 성능이나 인간의 읽기 쉬움까지 합격 처리하지 않는다.

원자료는 `artifacts/observer-frame-yield-browser-20260924.json`이다. 집계와 가장 느린 LoAF, 시작 프레임 귀속을 발췌했으며 전체 trace가 아니다. 후속 x1 전체 실행은 다음 절에서 확인했다. **동일 조건 3회 시작 heap/자료 수명·회수, 지도·자연 희귀 목표·다양한 합류/빈 슬롯·실제 계정 저장·사람의 재평가는 남는다.** 이전 x8 세 번의 시작 heap 증가 기록은 그대로 유지한다. 새 원본 + 서로 다른 배속 재경기를 그 메모리 관문의 대체 증거로 사용하지 않는다.

## 후속 x1 전체 실행

같은 `658f6582…` 제품 서버/브라우저를 새로고침하지 않고 17:50:10 원본을 보관함에서 복원했다. 처음부터 끝까지 x1이며, 측정 중 엔진 변경·무거운 검사/빌드·강제 GC는 하지 않았다. 원자료는 `artifacts/observer-frame-yield-x1-browser-20260924.json`이다.

| 측정 창 | Long Task 수 / 합 / 최대 | LoAF 최대 | RAF median / p95 / max | click→다음 RAF 표본 / 최대 | heap 시작 → 끝 |
| --- | --- | ---: | --- | --- | --- |
| 1162.0186초 | 34 / 2814 / **155ms** | **166.6ms** | 10 / 10.1 / 170.1ms | 10 / 10ms | 36,375,650 → 49,596,639B |

115,898개 RAF 간격, 종료 DOM 654개, visibility `visible`이다. 측정 시간에는 시작 준비/종료 뒤 체류가 포함되므로 게임 실행 시간과 구분한다. 단계별 최대는 초기 자원 33.7ms·페이즈 준비 53.8ms·전체 성장 155.3ms이며 중첩 단계의 합을 전체 시간으로 더하지 않는다.

- 제품 결과는 **18:14.25·7팀 2/3명·LP 0**, **사건 5,851개·최종 상태·종료 판정 모두 원본과 일치**다. 종료 후 보관함 8건, 최신 원본 한 건 및 이전 일곱 건의 호환성 차단 유지, 콘솔 warning/error 0, 평가 미시작을 확인했다. 진단을 중지하고 JSON/보관함을 닫았다.
- 경기 중 1팀→6팀→7팀 선택, 최근 사건 펼침/접기, 경기 현황→전체 로그→킬→전투 상세→요약→팀 관전 전환을 실제 수행했다. click 계측 10건에 모든 native select 변경이 포함됐다고 주장하지 않는다. 전투 상세의 1대 3 인원·HP·장비·전력 비교와 10:00 생명의 나무 채집/11:30 미스릴 구매 표시는 확인했지만 자연 자원 목표 카드의 활성 증거는 아니다.
- 이전 x8/x32와 이번 x1의 개별 Long Task는 모두 이 입력의 <200ms 기준을 통과했다. **반복 메모리 관문은 아직 미완료**다. x1의 시작 36.38MB/종료 49.60MB는 다른 배속/관전 입력의 한 표본이며, 기존 동일 x8 3회 시작 heap 증가를 해소했다는 증거로 쓰지 않는다. 실제 painted FPS·보편 입력 지연·사람의 가독성 합격과도 구분한다.

## 메모리 조사 범위

현재 보관함 목록 함수는 각 경기의 ID·시각·요약·시드·호환성만 UI 목록으로 반환한다. 실제 재경기는 한 기록을 읽고 `SimulationSession`의 key를 바꾸어 새 세션으로 전환한다. 진단 패널의 interval/click listener는 unmount 시 정리된다. 이 소스 확인만으로 브라우저의 객체 회수나 누수 부재를 증명하지 않는다. 앞선 세 번의 시작 heap 증가와 종료 heap 약 42MB는 여전히 별도 미확정 항목이며, 이번 표시 대기 수정으로 해결했다고 주장하지 않는다. 보관된 기록을 삭제하지 않는다.

2026-09-24 후속 읽기 전용 수명 감사:

| 보유 경로 | 확인한 수명/상한 | 증거 경계 |
| --- | --- | --- |
| 경기 입력·현재 프레임·저장 결과 | `useSimulationReplay`의 세션별 ref, 부모 `page.js`의 현재 기록 한 개와 key 교체 | 이전 React 트리의 실제 GC 완료를 측정한 것은 아님 |
| 보관함 | `simulationReplayStorage`는 cursor로 읽은 전체 기록 대신 메타데이터를 목록에 반환, load/save/list 완료 시 DB close, 보관 상한 10개 | DB에 남겨 둔 사용자의 원본을 지우지 않음 |
| 관전 진단 | RAF·입력 RAF·timer·PerformanceObserver·작업 구독을 stop에서 정리, histogram은 고정 크기, 느린 프레임/스크립트 각 8개·작업 단계 24종 상한 | 수집 자료는 scalar 사본이며 전체 trace/heap snapshot이 아님 |
| 자동 진행·UI 구독 | auto timeout, toast timeout, log RAF, 오류/키보드/click listener의 cleanup 경로 확인 | 진행 중 경기의 강제 unmount/중단 내구성을 별도 실행한 것은 아님 |
| 모델 조회 cache | 필드 공급원·카탈로그·팀 상태는 WeakMap 키를 사용 | WeakMap 사용만으로 다른 강한 참조가 없다고 증명하지 않음 |
| 지도 배치 cache | `minimapRegionLayoutRuntime`의 모듈 Map은 최대 256개, key는 구역/배치 형상, value는 x/y/scale만 보유 | 팀원·HP·경기 상태를 저장하지 않으나 실제 heap의 각 객체별 retained size는 미측정 |

감사 중 엔진·전투·난수·저장 구조는 변경하지 않았다. [MDN의 `performance.memory` 설명](https://developer.mozilla.org/en-US/docs/Web/API/Performance/memory)에 따라 이 API는 비표준이며 공유 heap 등으로 페이지별 사용량을 정확하게 분리하지 못할 수 있다. 이 한계는 기존 증가 표본을 없애거나 메모리 합격으로 바꾸는 근거가 아니다. 동일 조건 연속 경기의 시작/종료 값과 실제 회수 여부는 별도 실측으로 판단한다.

## 공개 반영

2026-09-24 기능 커밋 `22ee9577815ff427e9b071c07988ff8930810414`를 main에 푸시했고 [Vercel 배포](https://vercel.com/kurokosis-projects/eternal-hunger/7Mnpobv4ZvD1VTskyuXGqSGzCgFu)의 success 상태를 확인했다. 익명으로 고정 [평가 주소](https://eternal-hunger.vercel.app/eternalhunger/evaluate)에 접속하여 GAME READY, 24명/8팀, 기준 시드 `1789192220412`, 단일 `평가 시작`, x32 기본 선택 및 console warning/error 0을 확인했다. 임시 공개 확인 탭은 닫았다. 이는 배포/초기화 확인이며 위 로컬 전체 경기 성능이나 사람의 관전 평가를 공개 사이트에서 다시 측정했다는 뜻은 아니다.
